// notification-status.enum.ts
export enum NotificationStatus {
    PENDING = 'pending',
    QUEUED = 'queued',
    PROCESSING = 'processing',
    SENT = 'sent',
    FAILED = 'failed',
    CANCELLED = 'cancelled',
    RETRY = 'retry',
}